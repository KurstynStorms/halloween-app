<h1 class = "header">🎃 Reviews 🎃</h1>
<div class = "card cards mx-auto d-block">
  <% @reviews.each do |review| %>
    <p>
      <%= link_to review.movie.title, review_path(review) %>
    </p>
  <% end %>
  </div>